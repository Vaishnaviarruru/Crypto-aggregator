# 🔧 Server Configuration
PORT=3000

# 🧠 Redis Configuration
# Use your Upstash Redis URL or local Redis
REDIS_URL=rediss://default:YOUR_UPSTASH_PASSWORD@YOUR_UPSTASH_DOMAIN.upstash.io:6379

# ⏱️ Cache time-to-live (in seconds)
CACHE_TTL_SECONDS=30

# 💰 Default query token (used when /tokens?q= is missing)
DEFAULT_QUERY=doge
